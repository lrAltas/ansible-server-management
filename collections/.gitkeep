# This directory is for Ansible collections (ansible_collections/ or direct).
# Keep empty for now or add collections via ansible-galaxy or requirements.yml later.
# Example: collections/ansible_collections/community/general
