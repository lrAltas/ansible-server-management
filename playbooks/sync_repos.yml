---
- name: Synchronize DNF/YUM repositories to local mirror using dnf_repo_sync role
  hosts: dnf_repo_servers
  become: true
  gather_facts: true
  roles:
    - role: dnf_repo_sync
      tags: [dnf, repo, sync]
