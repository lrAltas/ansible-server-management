---
# Ansible Galaxy requirements
# Run: ansible-galaxy install -r requirements.yml

collections:
  - name: ansible.posix
    version: ">=1.5.0"
  - name: community.general
    version: ">=8.0.0"
  # Add more as needed, e.g. community.crypto for certs, nginxinc.nginx etc.
